# liqd-bar scripts

`power-menu.sh` is a fallback power menu used only when the distribution-level Rofi power menu is not available at `/etc/nixos/configs/rofi/scripts/power-menu.sh`.

The screenshot button intentionally calls `/etc/nixos/configs/hypr/modules/screenshot.sh` so it stays aligned with the existing Hyprland screenshot stack.
